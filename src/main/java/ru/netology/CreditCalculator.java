package ru.netology;

import java.math.BigDecimal;

public class CreditCalculator {

    public final static int ROUNDING_MODE = BigDecimal.ROUND_HALF_UP;

    public static BigDecimal monthlyPayment(int sum, int month, double percent) {
        return new BigDecimal(-1);
    }

    public static BigDecimal totalReturnedSum(int sum, int month, double percent) {
        return new BigDecimal(-1);
    }

    public static BigDecimal totalOverpayment(int sum, int month, double percent) {
        return new BigDecimal(-1);
    }
}
